{
    "$schema": "http://json-schema.org/draft-07/schema",
    "default": {},
    "description": "The root schema comprises the entire JSON document.",
    "examples": [
        {
            "primaryId": "shawn",
            "username": "oneilsh"
        }
    ],
    "required": [
        "primaryId",
        "username"
    ],
    "title": "The root schema",
    "type": "object",
    "properties": {
        "primaryId": {
            "$id": "#/properties/primaryId",
            "default": "",
            "description": "The primary Discovery Index ID associated with the user (unique static identifier, e.g. ORCiD, SSN, etc).",
            "examples": [
                "user@primary.email.com",
                "0185130498"
            ],
            "title": "The primaryId schema",
            "type": "string"
        },
        "username": {
            "$id": "#/properties/username",
            "default": "",
            "description": "User's github handle. May include @ prefix or not.",
            "examples": [
                "oneilsh",
                "@oneilsh"
            ],
            "title": "The github username schema",
            "type": "string"
        }
    }
}
